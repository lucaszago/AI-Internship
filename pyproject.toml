[project]
name = "week1v2-ask"
version = "0.1.0"
description = "Structured Q&A API with RAG (Pinecone) and Render deployment"
requires-python = "==3.12.*"
dependencies = [
    "fastapi[standard]",
    "httpx",
    "openai",
    "python-dotenv",
    "pinecone",
    "langchain-text-splitters",
    "streamlit",
    "uvicorn[standard]",
    "google-adk>=2.1.0",
]

[tool.uv]
package = false
